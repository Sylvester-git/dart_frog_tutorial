name: todo_server
description: A new Dart Frog application
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.1.0
  json_annotation: ^4.6.0
  todos_data_source:
    path: packages/todos_data_source
  todos_data_source_impl:
    path: packages/todos_data_source_impl

dev_dependencies:
  build_runner: ^2.2.0
  json_serializable: ^6.3.1
  mocktail: ^1.0.3
  test: ^1.25.5
  very_good_analysis: ^5.1.0
